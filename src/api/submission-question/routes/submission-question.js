'use strict';

/**
 * submission-question router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::submission-question.submission-question');
