'use strict';

/**
 * submission-question service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::submission-question.submission-question');
